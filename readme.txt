It's a loan status prediciton model 

Predicts whether the application for loan will be accepted or rejected based on features like income, education etc

train_u6l   -> it the csv file which consists the training and testing data

Loan_Status_prediction.ipynb -> It contains the coding in python(visualization, model building)

model.pkl -> it the a pickle file (contains the best model for prediction)

app.py -> model deployment file (run it from the terminal using following command)

streamlit run app.py
